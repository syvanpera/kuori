# regenerated by quickshell on every config load, with a per-shell-id path
.qmlls.ini

# working notes and agent configuration: local to this machine, deliberately not
# published. they were in the history until 2026-09-21 and were rewritten out of it.
CLAUDE.md
.claude/
