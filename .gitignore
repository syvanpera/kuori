# regenerated by quickshell on every config load, with a per-shell-id path
.qmlls.ini

# what points into the user's own accounts, and one person's agent permissions
CLAUDE.local.md
.claude/settings.local.json
